akka {

  loglevel = INFO
  
  actor {
    debug {
      # enable function of LoggingReceive, which is to log any received message at
      # DEBUG level
      # receive = on
    }
    
    entry-dispatcher {
      mailbox-type = "se.marfok.kakurosolver.akka.EntryMailbox"
    }
  }
}
